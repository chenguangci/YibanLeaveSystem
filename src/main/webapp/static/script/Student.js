

/*
 * var ,end;
 * 
 * laydate.render({ elem : '#', type : 'datetime', change : function(value,
 * date) { // 监听日期被切换 =value;
 * 
 *  } });
 * 
 * laydate.render({ elem : '#end', type : 'datetime', change : function(value,
 * date) { // 监听日期被切换 end =value; var end1=moment().format("end"); var
 * begin1=moment().format(""); if(end1<=begin1) {
 * layer.msg("开始时间不能在截止时间之后,请重新选择",{icon:2});
 * 
 * $("#end").onfocus(); } } });
 */
