<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0 //EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yiban.mapper.ClassMapper">
    <select id="searchTeacherByStudentId" resultType="java.lang.String">
        SELECT teacher_id FROM class WHERE id = #{id}
    </select>
    <select id="searchTeacher" resultType="java.util.List">
        SELECT id FROM class WHERE teacher_id = #{id}
    </select>
</mapper>